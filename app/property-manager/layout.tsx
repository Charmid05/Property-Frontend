import { PropertyManagerLayout } from "@/app/components/layouts/layouts";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <PropertyManagerLayout>{children}</PropertyManagerLayout>;
}
